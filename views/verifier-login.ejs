<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verifier Login</title>
    <link rel="stylesheet" href="verifier-login.css">
</head>
<body>
    <div class="banner"> <!-- Banner div for overlay -->
        <div class="overlay"></div> <!-- Add the overlay div here -->
        <div class="login-container">
            <h1>Verifier Login</h1>
            <form action="/verifier-login" method="POST">
                <input type="text" name="username" placeholder="Username" required>
                <input type="password" name="password" placeholder="Password" required>
                <button type="submit" class="button">Login</button>
            </form>
            <button class="button" id="signUpButton">Sign Up</button> <!-- New Sign Up button -->
        </div>
    </div>
    <script>
        document.getElementById('signUpButton').addEventListener('click', function() {
            window.location.href = '/verifier-signup'; // Redirects to the Sign Up page
        });
    </script>
</body>
</html>
